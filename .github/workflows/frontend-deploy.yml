name: 🎨 Frontend Deploy to Vercel

# Trigger on push to main branch (frontend changes only)
on:
  push:
    branches: [main, master]
    paths:
      - 'frontend/**'
      - '.github/workflows/frontend-deploy.yml'
  workflow_dispatch: # Allow manual trigger

jobs:
  deploy:
    name: Deploy Frontend
    runs-on: ubuntu-latest
    
    steps:
      # 1. Checkout code
      - name: 📥 Checkout code
        uses: actions/checkout@v4

      # 2. Setup Node.js
      - name: 🔧 Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      # 3. Install dependencies
      - name: 📦 Install dependencies
        run: |
          cd frontend
          npm install

      # 4. Run tests before deploy
      - name: 🧪 Run frontend tests
        run: |
          cd frontend
          npm test

      # 5. Build production bundle
      - name: 🏗️ Build for production
        env:
          VITE_API_URL: ${{ secrets.BACKEND_URL }}/api
        run: |
          cd frontend
          npm run build

      # 6. Deploy to Vercel
      - name: 🚀 Deploy to Vercel
        working-directory: ./frontend
        env:
          VERCEL_TOKEN: ${{ secrets.VERCEL_TOKEN }}
          VERCEL_ORG_ID: ${{ secrets.VERCEL_ORG_ID }}
          VERCEL_PROJECT_ID: ${{ secrets.VERCEL_PROJECT_ID }}
        run: |
          # Create .vercel folder with project config to override Vercel settings
          mkdir -p .vercel
          echo '{"orgId":"'$VERCEL_ORG_ID'","projectId":"'$VERCEL_PROJECT_ID'"}' > .vercel/project.json
          # Deploy from current directory (frontend/) - Vercel will use current dir as root
          npx vercel deploy --prod --token $VERCEL_TOKEN --yes

      # 7. Notify on success
      - name: ✅ Deployment successful
        if: success()
        run: |
          echo "✅ Frontend deployed successfully!"
          echo "🔗 URL: Check Vercel dashboard"

